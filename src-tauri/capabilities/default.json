{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main OpenMinutes window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
